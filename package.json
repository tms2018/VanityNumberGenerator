{
  "name": "voicefoundry-test",
  "description": "VoiceFoundry test",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "aws-sdk": "^2.799.0",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "aws-sdk-mock": "^5.1.0",
    "jest": "^26.6.3"
  },
  "scripts": {
    "test": "jest --roots __tests__/unit"
  }
}
